import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fw-ng-alert-toast-a11y',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
